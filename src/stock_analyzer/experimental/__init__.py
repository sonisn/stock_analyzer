"""Experimental modules — opt-in features that aren't part of the main pipeline.

Currently:
  - dashboard.py — Streamlit dashboard for browsing past runs.
    Install with `uv pip install -e ".[dashboard]"` then
    `streamlit run src/stock_analyzer/experimental/dashboard.py`.
"""
